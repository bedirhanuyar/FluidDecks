<ui:FluentWindow x:Class="FluidDecks.UI.Windows.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="FluidDecks Input" Width="300"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <ui:TitleBar Title="Rename" Grid.Row="0" />
        
        <Grid Grid.Row="1" Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="MessageText" Text="Enter new name:" Margin="0,0,0,10"/>
        <TextBox x:Name="InputTextBox" Grid.Row="1" Background="#303030" Foreground="White" BorderThickness="0" Padding="5"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="OkButton_Click" Width="70" Margin="0,0,10,0"/>
            <Button Content="Cancel" Click="CancelButton_Click" Width="70"/>
        </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
